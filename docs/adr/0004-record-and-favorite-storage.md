---
status: accepted
---

# 收藏和播放记录按服务存储能力同步

应用优先通过 MoonTVPlus 服务端的收藏与播放记录 API 与网页双向同步。当用户的服务实例采用浏览器 localStorage 模式时，网页数据不在服务端，原生应用无法读取，因此电视改为本机保存，并明确显示不能跨设备同步。相较于强制要求服务端数据库或始终仅本机保存，这一方案保留了对不同自部署方式的支持，同时让支持同步的用户获得连续观看体验。
